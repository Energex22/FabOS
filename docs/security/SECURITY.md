# Security
Windows 7 is unsupported. Keep FabOS on a trusted LAN, bind to localhost by default, never expose it directly to the internet, protect API keys, validate uploaded files and archive paths, require confirmation before unattended starts, and encrypt customer-data backups when moved off the machine.
